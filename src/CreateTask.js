import { useState } from "react";

function CreateTask() {
    return (
        <form>
          <input type="text" />
          <button type="submit">Add Task</button>
        </form>
    );
}

export default CreateTask;